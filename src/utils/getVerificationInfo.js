// Helper function for retrieving stored verification data
const getVerificationInfo = async (verificationId) => {
  // Validate the verification info with external API as needed
  return true
}
module.exports = { getVerificationInfo }
